---
description: Increase confidence in a memory (positive feedback)
---

Use the `ccmem-reinforce` skill to handle this command. Run `ccmem reinforce <id>` to increase the memory's confidence.

$ARGUMENTS
