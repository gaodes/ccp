---
description: Run maintenance on memory system
---

Use the `ccmem-cleanup` skill to handle this command. Run `ccmem cleanup` to archive low-confidence memories, merge duplicates, and perform maintenance tasks.

$ARGUMENTS
