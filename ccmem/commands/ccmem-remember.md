---
description: Create a memory from natural language or structured flags
---

Use the `ccmem-remember` skill to handle this command. Parse the user's input and run `ccmem create` to store the memory.

$ARGUMENTS
